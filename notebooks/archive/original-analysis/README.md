# Original analysis

`python_uk_bands.ipynb` is the project's first working scratchpad. It is kept
for provenance and contains early API experiments, cached-data exploration and
the original shortlist analysis.

It is not part of the active pipeline and is not expected to execute cleanly
from top to bottom. Use the notebook under `notebooks/final/` for the current
reader-facing analysis.
